---
import ThemeIcon from "./ThemeButton.astro";

interface Props {
  className?: string;
}
const { className } = Astro.props;
---

<header class={"mt-4 mb-12 " + className}>
  <nav class="flex gap-4">
    <a href="/" class="">Home</a>
    <ThemeIcon className="ml-auto" />
  </nav>
</header>
